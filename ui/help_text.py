"""
Текст справки, ChangeLog, README и Руководство пользователя для SQL Metadata Parser v5.0.
"""

HELP_TEXT = """
SQL Metadata Parser v5.0

ОСНОВНЫЕ ВОЗМОЖНОСТИ:

1. АНАЛИЗ SQL ЗАПРОСОВ
   • Автоматический парсинг SQL запросов через sqlglot
   • Прогрессивный парсинг — пошаговая очистка с автоисправлением ошибок
   • Поддержка 14+ SQL диалектов (Oracle, PostgreSQL, MySQL, SQL Server и др.)
   • Обработка сложных запросов с JOIN, подзапросами, CTE
   • Извлечение таблиц, колонок, алиасов, схем, типов JOIN

2. МОЩНЫЙ ПРЕПРОЦЕССОР SQL (21 шаг очистки)
   • Удаление комментариев с учётом строковых литералов
   • Обработка Oracle hints, outer join (+), T-SQL GO, PL/SQL блоков
   • Замена XML-блоков <Procedure>, TABLE() функций, ODBC {fn...}
   • Конвертация CONVERT -> CAST, @переменных, TO_DATE
   • Исправление незакрытых CASE, неполных BETWEEN, несбалансированных скобок
   • Конвертация IF() -> CASE WHEN

3. ВИЗУАЛИЗАЦИЯ РЕЗУЛЬТАТОВ
   • Табличное представление данных
   • Группировка по таблицам
   • Типы таблиц (таблица, CTE, подзапрос, процедура)
   • Типы JOIN (LEFT, RIGHT, INNER, OUTER)
   • Статистика анализа
   • Текстовый вывод с детализацией

4. ЭКСПОРТ ДАННЫХ
   • Сохранение в Excel формате
   • Экспорт в JSON, CSV, TXT
   • Копирование в буфер обмена

5. РАБОТА С ФАЙЛАМИ
   • Загрузка SQL из файлов (.sql, .txt)
   • Автоопределение кодировки
   • Примеры SQL запросов
   • Очистка полей ввода

ИНСТРУКЦИЯ ПО ИСПОЛЬЗОВАНИЮ:
1. Введите SQL запрос в левое поле или загрузите из файла
2. Выберите диалект SQL (по умолчанию Oracle)
3. Нажмите "Анализировать SQL" для обработки
4. Просмотрите результаты во вкладках справа
5. Экспортируйте данные в нужном формате

ГОРЯЧИЕ КЛАВИШИ:
  Ctrl+Enter — Анализ SQL
  Ctrl+S     — Экспорт CSV

Разработчик: @BDV_80 (Береговой Дмитрий)
Версия: 5.0 (PyQt6)
"""

CHANGELOG_TEXT = """
ChangeLog — SQL Metadata Parser

═══════════════════════════════════════════════
 2026-07-12 — Обновление v5.0: перенос улучшений из BO
═══════════════════════════════════════════════

core/sql_preprocessor.py — полная переработка препроцессора

  Новые шаги очистки (21 шаг вместо 7):
  • Удаление комментариев (--, /* */) с учётом строковых литералов
  • Удаление Oracle hints: /*+ ... */
  • Удаление outer join синтаксиса: (+)
  • Удаление T-SQL batch separator: GO
  • Удаление PL/SQL блоков: CREATE FUNCTION, BEGIN...END
  • Замена <Procedure> XML-блоков -> schema."name"
  • Замена TABLE() функций -> func
  • Замена ODBC {fn ...} функций
  • Исправление незакрытых CASE
  • Исправление неполных BETWEEN
  • Исправление обрезанных выражений (IS, LIKE, операторы)
  • Исправление проблем CTE
  • Конвертация IF() -> CASE WHEN
  • Удаление пустых скобок
  • Удаление лишних ) и AS после алиасов подзапросов
  • Балансировка несбалансированных скобок

  Оптимизации:
  • Скомпилированные regex для производительности
  • Генератор preprocess_stepwise() для пошаговой обработки

core/parser_strategy.py — прогрессивный парсинг

  Новый класс ProgressiveSQLGlotParserStrategy:
  • Пошаговый парсинг: сырой SQL -> каждый шаг очистки
  • Автоисправление: несбалансированные скобки, CASE, BETWEEN
  • Запоминание применённых шагов

models/sql_metadata.py — расширение моделей

  • TableType.PROCEDURE — тип таблицы "Процедура"
  • SQLMetadata.procedures — данные о процедурах
  • SQLMetadata.table_functions — данные о TABLE() функциях

core/column_analyzer.py — оптимизация производительности

  • _cte_names: Set[str] — быстрый поиск CTE
  • _table_name_index: Dict — O(1) поиск таблицы по имени
  • Обработка catalog.schema для полных имён схем

core/parser_factory.py — поддержка прогрессивного парсера

  • Тип "progressive" в ParserFactory.create_parser()

═══════════════════════════════════════════════
 2026-06-01 — Оптимизация анализа колонок
═══════════════════════════════════════════════

  • _cte_names: Set[str] — быстрый поиск CTE
  • _table_name_index: Dict[str, TableInfo] — O(1) поиск таблицы
  • Обработка catalog.schema

═══════════════════════════════════════════════
 2026-05-15 — Тип JOIN и Oracle outer join
═══════════════════════════════════════════════

  • Добавлен join_type в TableInfo (LEFT, RIGHT, INNER, OUTER)
  • Детектирование Oracle outer join (+)
  • TableType.PROCEDURE в enum типов таблиц

═══════════════════════════════════════════════
 2026-04-01 — Рефакторинг в модульную архитектуру
═══════════════════════════════════════════════

  • Объединение sql_highlighter.py с main_window.py
  • Вынос текста справки в ui/help_text.py
  • Перемещение SQLDialect и SQLPreprocessor в core/
  • Обновление импортов
"""

USER_GUIDE_TEXT = """
Руководство пользователя SQL Metadata Parser v5.0

ВВЕДЕНИЕ

SQL Metadata Parser — инструмент для анализа SQL запросов, который
автоматически извлекает метаданные: таблицы, колонки, алиасы, контексты
использования и зависимости. Приложение на PyQt6, поддерживает 14+
диалектов SQL.

ЧТО НОВОГО В v5.0:
  • Прогрессивный парсинг — пошаговая очистка SQL с автоисправлением ошибок
  • 21 шаг препроцессинга
  • Автоисправление — незакрытые скобки, CASE, BETWEEN
  • Типы JOIN — LEFT, RIGHT, INNER, OUTER
  • Типы таблиц — таблица, CTE, подзапрос, процедура

УСТАНОВКА И ЗАПУСК

  Требования: Python 3.10+, зависимости из requirements.txt

  Установка:
    pip install -r requirements.txt

  Запуск:
    python app.py

ИНТЕРФЕЙС ПРИЛОЖЕНИЯ

  Левая панель — ввод SQL:
    • Поле ввода SQL — вставьте или введите запрос
    • Пример SQL — загружает пример (циклически переключается)
    • Загрузить — загружает SQL из файла (.sql, .txt)
    • Очистить — очищает поле и результаты
    • Подсветка синтаксиса — вкл/выкл цветовую подсветку
    • Выбор диалекта — Oracle, PostgreSQL, MySQL, SQL Server, Snowflake
    • Анализировать SQL — запускает анализ

  Правая панель — результаты:
    • Общий фильтр — фильтрация по всем вкладкам
    • Вкладки: Колонки, Таблицы, Линедж, Статистика, Текстовый вывод
    • Экспорт: CSV, Excel, TXT, JSON, копирование в буфер

  Верхняя панель:
    • Тёмная тема — переключатель темы
    • Справка — окно справки с вкладками

ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

  1. Быстрый старт:
     Вставьте SQL -> выберите диалект -> нажмите "Анализировать SQL"

  2. Сложный запрос:
     Нажмите "Пример SQL" для загрузки запроса с CTE, подзапросами,
     алиасами и агрегатными функциями

  3. Фильтрация:
     Введите текст в "Общий фильтр" для фильтрации по всем вкладкам

  4. Экспорт:
     Используйте кнопки CSV/Excel/TXT/JSON или Ctrl+S для CSV

  5. Автоисправление SQL:
     Прогрессивный парсер автоматически исправляет:
     - Незакрытые скобки
     - Незакрытый CASE
     - Неполный BETWEEN
     - Oracle hints, outer join (+), PL/SQL блоки
     - CONVERT -> CAST, @переменные

ГОРЯЧИЕ КЛАВИШИ:
  Ctrl+Enter — Анализ SQL
  Ctrl+S     — Экспорт CSV

ПОДДЕРЖИВАЕМЫЕ ДИАЛЕКТЫ:
  Oracle, PostgreSQL, MySQL, SQL Server, Snowflake, BigQuery,
  ClickHouse, DuckDB, Redshift, Databricks, Hive, Presto,
  Trino, Teradata, SQLite

ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ:

  В: Может ли парсер обрабатывать CTE и подзапросы?
  О: Да. CTE = тип "CTE", подзапросы = тип "Подзапрос".

  В: Что делать при ошибке?
  О: 1) Попробуйте прогрессивный парсер
     2) Проверьте синтаксис для диалекта
     3) Сообщите о проблеме с текстом запроса

  В: Как интерпретируются вычисляемые колонки?
  О: Флаг is_calculation + тип вычисления + исходное выражение.

  В: Можно ли анализировать несколько запросов?
  О: Нет, один за раз. Используйте UNION или анализируйте поочерёдно.

Разработчик: @BDV_80 (Береговой Дмитрий)
Версия: 5.0 (PyQt6)
Лицензия: MIT
"""

README_TEXT = """
SQL Metadata Parser v5.0

Инструмент для анализа SQL-запросов, извлечения метаданных таблиц
и колонок с поддержкой различных диалектов SQL.

ВОЗМОЖНОСТИ:
  • Автоматический парсинг SQL через sqlglot
  • Прогрессивный парсинг — пошаговая очистка с автоисправлением
  • Поддержка 14+ диалектов SQL
  • Извлечение таблиц, колонок, алиасов, схем, типов JOIN
  • Мощный препроцессор (21 шаг очистки)
  • Визуализация через древовидные виджеты и вкладки
  • Экспорт в Excel, JSON, CSV, TXT
  • Подсветка синтаксиса SQL

АРХИТЕКТУРА ПРОЕКТА:

  Основные модули:
    1. app.py                  — точка входа
    2. core/sql_parser.py      — фасад парсера
    3. core/sql_preprocessor.py — препроцессор (21 шаг)
    4. core/parser_strategy.py  — стратегии: sqlglot, progressive
    5. core/parser_factory.py   — фабрика парсеров
    6. core/column_analyzer.py  — анализатор колонок (O(1) поиск)
    7. core/export_manager.py   — менеджер экспорта
    8. models/sql_metadata.py   — модели данных
    9. ui/main_window.py        — главное окно PyQt6
   10. ui/help_text.py          — текст справки
   11. tests/                   — тесты

УСТАНОВКА И ЗАПУСК:

  Требования: Python 3.10+

  Установка:
    pip install -r requirements.txt

  Запуск:
    python app.py

  Тесты:
    pytest tests/ -v

ПРОГРАММНОЕ ИСПОЛЬЗОВАНИЕ:

  from core.parser_factory import ParserFactory
  from core.sql_dialect import SQLDialect

  # Стандартный парсер
  parser = ParserFactory.create_parser("sqlglot", dialect=SQLDialect.ORACLE)
  metadata = parser.parse("SELECT a, b FROM users WHERE id = 1")

  # Прогрессивный парсер (автоисправление ошибок)
  parser = ParserFactory.create_parser("progressive", dialect=SQLDialect.ORACLE)
  metadata = parser.parse("SELECT a FROM (SELECT b FROM t1")

  print(f"Колонки: {len(metadata.columns)}")
  print(f"Таблицы: {len(metadata.tables)}")
  print(f"Ошибки: {metadata.parse_errors}")
  print(f"Шаги: {parser.last_applied_steps}")

СБОРКА EXE:

  1. Перейдите в папку for_exe
  2. Запустите build_exe.bat
  3. Готовый SQLparser.exe появится в корне проекта

  Ручная сборка:
    pyinstaller --clean --log-level INFO SQLparser_hook.spec

ИЗМЕНЕНИЯ v5.0 (июль 2026):
  1. Прогрессивный парсер — пошаговая очистка SQL
  2. 21 шаг препроцессинга
  3. O(1) поиск таблиц
  4. Типы JOIN — LEFT, RIGHT, INNER, OUTER
  5. Procedure и TABLE() — обработка XML-блоков

ТЕСТИРОВАНИЕ:
  • tests/test_sql_preprocessor.py — 5 тестов
  • tests/test_column_analysis.py — 5 тестов
  • tests/test_join_type_feature.py — 6 тестов

Разработчик: @BDV_80 (Береговой Дмитрий)
Версия: 5.0 (PyQt6)
Лицензия: MIT

Благодарности:
  • sqlglot — парсинг SQL
  • PyQt6 — графический интерфейс
  • pandas — обработка данных и экспорт в Excel
"""
